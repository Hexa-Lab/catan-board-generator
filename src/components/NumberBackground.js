import { Text } from "react-hexgrid";

const NumberBackground = () => {
    return <Text 
        fontSize={65}
        stroke="black"
        strokeWidth={0.1}
        fill="tan"
        style={{ transform: "translateY(-16.5px)" }}>.</Text>
}

export default NumberBackground;