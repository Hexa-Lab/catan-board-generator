import { Text } from "react-hexgrid";

const NumberTileBackground = () => {
    return <Text 
        fontSize={65}
        stroke="black"
        strokeWidth={0.1}
        fill="tan"
        style={{ transform: "translateY(-16.5px) translateX(0.25px)" }}>.</Text>
}

export default NumberTileBackground;